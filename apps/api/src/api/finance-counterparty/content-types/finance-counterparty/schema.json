{
  "kind": "collectionType",
  "collectionName": "finance_counterparties",
  "info": {
    "singularName": "finance-counterparty",
    "pluralName": "finance-counterparties",
    "displayName": "Finance Counterparty",
    "description": "Clients and vendors"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "workspace_id": {
      "type": "biginteger",
      "required": true
    },
    "name": {
      "type": "string",
      "required": true
    },
    "kind": {
      "type": "enumeration",
      "enum": ["CLIENT", "VENDOR", "BOTH", "UNKNOWN", "EMPLOYEE"],
      "required": true,
      "default": "UNKNOWN"
    },
    "notes": {
      "type": "text"
    }
  }
}
