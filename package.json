{
  "name": "tkturners-accounting",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web \"yarn workspace api develop\" \"yarn workspace web dev\"",
    "dev:web": "yarn workspace web dev",
    "dev:api": "yarn workspace api develop",
    "build": "yarn workspace api build && yarn workspace web build",
    "test": "yarn workspace @tkturners/parser test && yarn workspace api test",
    "lint": "yarn workspace web lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
